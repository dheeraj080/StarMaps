export const UNIVERSE_SCALE = Math.pow(10, -4.2);
export const CELESTIAL_SCALE = Math.pow(10, -3.8);
export const ORBIT_SCALE = UNIVERSE_SCALE;

export const DEGREES_TO_RADIANS_RATIO = Math.PI / 180;
export const RADIANS_TO_DEGREES_RATIO = 180 / Math.PI;

